/**
 * 절약 팁 생성 로직
 * Design Ref: §5.3 — 주요 절약 시나리오 2~3개 생성
 * NOTE: v1 logic removed. Will be rebuilt in Phase 3.
 */

import type { SavingTip } from '@/types/calculator';

export function generateSavingTips(): SavingTip[] {
  return [];
}
